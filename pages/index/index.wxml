<!--index.wxml-->
<view class="container">
  <view class="tabNav">
    <view wx:for="{{navTab}}" wx:key="index"  bindtap="currentTab" class="{{currentTab==index ? 'cur' : ''}}"><text>{{item}}</text></view>
  </view>
  <list></list>
</view>
